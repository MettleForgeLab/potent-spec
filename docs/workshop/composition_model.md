# composition_model

Potents do not form pipelines.

No shared state exists between Potents.

Each interaction is a discrete boundary event.

Outputs must be externally re-declared as new inputs.

No implicit linkage exists between sequential Potents.

Composition occurs through repeated resets, not continuity.

Each Potent operates independently of prior interactions.
