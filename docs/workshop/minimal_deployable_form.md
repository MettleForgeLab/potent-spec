# minimal_deployable_form

A Potent may exist as a single boundary event.

The core is invoked, not created.

The membrane is declared per interaction.

Validation occurs before any action.

Input is explicitly defined.

Output is either passed or refused.

No state persists after execution.

The boundary dissolves completely after the event.
