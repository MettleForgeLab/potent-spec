# multi_attachment_traversal

Multiple attachments are accessed through separate channels.

No implicit merging of attachments is permitted.

Each attachment retains its independent meaning.

The membrane may move between attachments.

Relations between attachments are constructed per interaction.

No shared context exists across attachments by default.

No composite payload is created.

Traversal is controlled, not blended.
