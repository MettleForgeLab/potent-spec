# usage_boundary

This repository defines a specification, not a system implementation.

It must not be used to introduce runtime behavior.

It must not be extended with application logic.

It must not be interpreted as a framework.

It must not be modified without versioned change.

All usage must treat SPECIFICATION.md as invariant.

All derived work must occur outside this repository.
